package com.example.wallet.entity;

/**
 * Represents the type of a transaction.
 *
 * For this assignment we only support DEBIT (money going out of the wallet).
 * You can add CREDIT later if needed.
 */
public enum TransactionType {
    DEBIT
}
